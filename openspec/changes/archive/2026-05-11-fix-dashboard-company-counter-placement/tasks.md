## 1. Implementation
- [x] 1.1 Update `templates/dashboard/index.html` to move `data-filter-widget` attribute from the `<form>` element to its parent `<div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6">` element so it encompasses the company counter.
- [x] 1.2 Verify that the company counter now successfully updates when selecting dropdown options in the dashboard.