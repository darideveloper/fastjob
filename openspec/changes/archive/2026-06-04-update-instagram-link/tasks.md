## 1. Implementation

- [x] 1.1 Update the Instagram URL in `apps/core/context_processors.py` to `https://www.instagram.com/fastjob.es`
- [x] 1.2 Verify the `aria_label` remains `FastJob en Instagram` or update if necessary

## 2. Verification

- [x] 2.1 Start the development server and check the footer link on the landing page
- [x] 2.2 Verify the link opens in a new tab and points to the correct profile
- [x] 2.3 Run existing tests to ensure no regressions in context processors or template rendering
