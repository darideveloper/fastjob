[pytest]
DJANGO_SETTINGS_MODULE = config.test_settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --tb=short --strict-markers
markers =
    slow: tests that touch external services (deselect with '-m "not slow"')
