# Dev-only dependencies. Not installed in the production image.
# Install in development environments via:  pip install -r requirements-dev.txt
-r requirements.txt

pytest==8.3.3
pytest-django==4.9.0
pytest-mock==3.14.0
